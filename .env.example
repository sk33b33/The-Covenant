# Copy to .env.local (already gitignored) and fill in your Supabase project's
# values. Both are optional — leaving them blank runs the game exactly as it
# does without an account: fully local, no cloud sync, no crash.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
